name: Build Artifacts

on:
  workflow_dispatch:
  pull_request:
    branches: [main]

jobs:
  build:
    permissions:
      contents: read
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6.0.3
      - uses: actions/setup-python@v6.2.0
        with:
          python-version: "3.11"
          cache: pip
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller
          pip install -e .
      - name: Build executable
        run: pyinstaller --noconfirm --clean --onefile --name github-pr-monitor src/main.py
      - name: Package artifact (Unix)
        if: runner.os != 'Windows'
        run: |
          # Use branch name for PRs (GITHUB_HEAD_REF), tag/branch for other triggers
          RAW="${GITHUB_HEAD_REF:-${GITHUB_REF_NAME:-snapshot}}"
          VERSION="${RAW//\//-}"  # replace any / with - so paths are safe
          PKG_NAME="github-pr-monitor-${RUNNER_OS}-${VERSION}.tar.gz"
          tar -czf "dist/${PKG_NAME}" -C dist github-pr-monitor
          shasum -a 256 "dist/${PKG_NAME}" > "dist/SHA256SUMS-${RUNNER_OS}.txt"
      - name: Package artifact (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          # Use branch name for PRs, tag/branch for other triggers; sanitize slashes
          $raw = if ($env:GITHUB_HEAD_REF) { $env:GITHUB_HEAD_REF } elseif ($env:GITHUB_REF_NAME) { $env:GITHUB_REF_NAME } else { 'snapshot' }
          $version = $raw -replace '/', '-'
          $pkgName = "github-pr-monitor-$env:RUNNER_OS-$version.zip"
          Compress-Archive -Path dist/github-pr-monitor.exe -DestinationPath "dist/$pkgName" -Force
          (Get-FileHash "dist/$pkgName" -Algorithm SHA256).Hash + "  $pkgName" | Out-File -FilePath "dist/SHA256SUMS-$env:RUNNER_OS.txt" -Encoding ascii
      - name: Upload artifact
        uses: actions/upload-artifact@v7.0.1
        with:
          name: app-${{ matrix.os }}
          path: |
            dist/*.tar.gz
            dist/*.zip
            dist/SHA256SUMS-*.txt
